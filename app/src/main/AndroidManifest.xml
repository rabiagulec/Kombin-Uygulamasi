<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tr.edu.yildiz.rabiagulecproje">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity android:name=".EtkinlikGuncelleActivity"></activity>
        <activity android:name=".EtkinlikEkleActivity" />
        <activity android:name=".KiyafetSecActivity" />
        <activity android:name=".KabinOdasiActivity" />
        <activity android:name=".KiyafetGuncelleActivity" />
        <activity android:name=".KiyafetEkleActivity" />
        <activity
            android:name=".CekmeceActivity"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".MenuActivity"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".SignUpActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>